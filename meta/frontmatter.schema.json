{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/frontmatter.schema.json",
  "title": "Knowledge document frontmatter",
  "type": "object",
  "required": ["id", "title", "description", "type", "status", "provenance"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9]+([._-][a-z0-9]+)*$" },
    "title": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1, "maxLength": 320 },
    "type": { "enum": ["concept", "reference", "record", "skill", "agent"] },
    "status": { "enum": ["active", "draft", "deprecated", "superseded"] },
    "provenance": { "enum": ["verified", "decided", "asserted"] },
    "domain": { "type": "string", "minLength": 1 },
    "owner": { "type": "string", "minLength": 1 },
    "timestamp": { "type": "string", "anyOf": [{ "format": "date" }, { "format": "date-time" }] },
    "last_verified": { "type": "string", "format": "date" },
    "scope": { "type": "array", "items": { "type": "string" } },
    "tags": { "type": "array", "items": { "type": "string" } },
    "supersedes": { "type": "array", "items": { "type": "string" } },
    "superseded_by": { "type": ["string", "null"] },
    "visibility": { "enum": ["internal", "restricted"] },
    "sources": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "repo": { "type": "string" },
          "path": { "type": "string" },
          "url": { "type": "string", "format": "uri" }
        }
      }
    },
    "skill": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
        "allowed-tools": { "type": "array", "items": { "type": "string" } }
      }
    },
    "agent": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
        "tools": { "type": "array", "items": { "type": "string" } },
        "model": { "type": "string" },
        "effort": { "enum": ["low", "medium", "high", "xhigh", "max"] },
        "color": { "type": "string" },
        "permission-mode": { "enum": ["default", "acceptEdits", "plan"] }
      }
    }
  },
  "allOf": [
    {
      "if": { "properties": { "type": { "enum": ["reference"] } } },
      "then": { "required": ["sources"], "properties": { "sources": { "minItems": 1 } } }
    }
  ]
}
