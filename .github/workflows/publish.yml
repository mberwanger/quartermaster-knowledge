name: publish

on:
  push:
    tags: ["v*"]

permissions:
  contents: read
  packages: write

env:
  QM_VERSION: v0.2.2
  REGISTRY: ghcr.io

jobs:
  publish:
    name: publish the bundle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: stable

      # Two things setup-go does not do for us. GOPATH/bin is not on PATH, and
      # go install names the binary after the module path, so it arrives as
      # quartermaster rather than qm. The release archives ship it as qm.
      - name: Install Quartermaster
        run: |
          go install github.com/mberwanger/quartermaster@${{ env.QM_VERSION }}
          mv "$(go env GOPATH)/bin/quartermaster" "$(go env GOPATH)/bin/qm"
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      # The same checks the pull request ran. A tag is not a reason to trust a
      # tree that was never validated: tags get moved, and branches get pushed to
      # after review.
      - name: Validate
        run: qm bundle validate --root .

      - name: Check listings
        run: qm bundle index --root . --check

      - name: Build
        run: qm bundle build --root . --out dist --repo ${{ github.repository }} --commit ${{ github.sha }}

      # Quartermaster carries no credentials of its own. It reads the Docker
      # credential store, so logging in here is what authenticates the push.
      - name: Log in to ${{ env.REGISTRY }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish
        run: |
          qm bundle publish \
            --dir dist \
            --ref ${{ env.REGISTRY }}/${{ github.repository }}:${{ github.ref_name }} \
            --tag latest

      - name: Report what was published
        run: |
          echo "### Published" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          jq -r '"digest   \(.digest)\ndocs     \(.docs)\npackages \(.packages)"' dist/meta.json >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
